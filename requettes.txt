uvicorn main:app --reload   ------------- lancer le serveur

python -m tests.test_mdp  ----------------- lancer le test
python -m tests.test_excel
python -m tests.test_sync
python -m tests.tes_mdp
python -m tests.test_product_service
python -m tests.test_type_detector
python -m tests.check_tables
python -m tests.check_metadata
python -m tests.test_cache
python -m tests.test_stats
python -m tests.migrate_to_postgres

python -m tests.export_school_lists_to_excel


Serveurs de noms externes

lola.ns.cloudflare.com
malcolm.ns.cloudflare.com
Modifier


Gandi LiveDNS


ns-238-a.gandi.net
ns-115-b.gandi.net
ns-101-c.gandi.net
ipc369commandes@gmail.com
ip369gabon*

3020120816180

ipc369commandes-design


# Initialiser Git dans votre dossier local
git init

# Ajouter tous les fichiers de votre projet au suivi Git
git add .
pour forcer Git a suivre et à enregistrer les modif de la base de donnée
git add -f data/erp.db

# Enregistrer votre premier commit de sauvegarde
git commit -m "Mise à jour base de donnees"

git rm -r --cached venv

git commit --amend --no-edit

git push -u origin main

git add votre_fichier_base.db
git commit -m "Repare pdf liste scolaire"
git push origin main


# 1. Indiquer à Git d'enregistrer le fichier .gitignore
git add .gitignore

# 2. Créer un commit de sauvegarde
git commit -m "Configure .gitignore pour exclure venv et les fichiers sensibles"

# 3. Envoyer la mise à jour sur GitHub
git push origin main



# 1. Ajouter spécifiquement le fichier du dossier backend
git add backend/requirements.txt

# 2. Enregistrer la modification
git commit -m "mise à jour base de donnée"

# 3. Envoyer sur GitHub
git push origin main
``` [3]

Une fois que vous aurez poussé ce commit, Render relancera automatiquement le déploiement. Vous verrez alors les lignes de téléchargement de `playwright`, de `fastapi` et de vos autres dépendances s'afficher, et l'installation de Chromium se fera correctement.














# Configurer la branche principale sur 'main'
git branch -M main

# Lier votre dossier local à votre dépôt GitHub (remplacez par l'URL de votre dépôt)
git remote add origin https://github.com/ipc369commandes-design/erp-mdp.git

# Envoyer le code sur GitHub
git push -u origin main


SUR RENDER

uvicorn main:app --host 0.0.0.0 --port $PORT
pip install -r requirements.txt && playwright install chromium




🔍 DETECTION MDP : 9782818702871
   Test type 1
🔄 Synchronisation en cours -⚠️ Type 1 : HTTP 500:
   Test type 5
🔄 Synchronisation en cours \⚠️ Type 1 : HTTP 500:
   Test type 5
🔄 Synchronisation en cours /   Test type 6
🔄 Synchronisation en cours -   Test type 6
💾 CACHE SAVE : 9782818907900 -> NOT_FOUND
🔍 DETECTION MDP : 9782818702840
   Test type 1
🔄 Synchronisation en cours \💾 CACHE SAVE : 9782014645392 -> NOT_FOUND
🔍 DETECTION MDP : 9782818703267
   Test type 1
🔄 Synchronisation en cours |⚠️ Type 1 : HTTP 500:
   Test type 5
🔄 Synchronisation en cours -   Test type 6
🔄 Synchronisation en cours \💾 CACHE SAVE : 9782818702871 -> NOT_FOUND
🔍 DETECTION MDP : 9782266245487
   Test type 1
🔄 Synchronisation en cours |⚠️ Type 1 : HTTP 500:
   Test type 5
⚠️ Type 1 : HTTP 500:
   Test type 5
🔄 Synchronisation en cours \   Test type 6
🔄 Synchronisation en cours |   Test type 6
💾 CACHE SAVE : 9782818702840 -> NOT_FOUND
🔍 DETECTION MDP : 9782266204965
   Test type 1
🔄 Synchronisation en cours /⚠️ Type 1 : HTTP 500:
   Test type 5
💾 CACHE SAVE : 9782818703267 -> NOT_FOUND
🔍 DETECTION MDP : 9782262029869
   Test type 1
🔄 Synchronisation en cours -   Test type 6


bouton à remettre:
BOUTON JS
 <button class="btn btn-info" id="importPlatformBtn" style="padding: 12px 20px; width: 100%;">🔄 Importer Plateforme</button>


Bouton HTML
  <button id="syncBtn" class="sync-btn">
     ⚙️ Lancer la synchronisation
  </button>

importBtn.textContent = "🔄 Importer Plateforme";



nettoyage du cache: 

python -c "from app.core.database import SessionLocal; from app.models.product_type_cache import ProductTypeCache; db=SessionLocal(); db.query(ProductTypeCache).delete(); db.commit(); print('🧹 Cache de détection réinitialisé avec succès !'); db.close()"


froce la mise à jour de la base de donnée:

git add -f data/erp.db

git commit -m "Forcer l'ajout de la base erp.db synchronisee"

git push origin main

# 1. Remontez à la racine si ce n'est pas déjà fait
cd G:\ipc\erp_mdp_v1

# 2. Forcez Git à ajouter le fichier avec son chemin complet depuis la racine
git add -f backend/data/erp.db

# 3. Vérifiez si Git l'a bien pris en compte
git status

# 4. Enregistrez et envoyez
git commit -m "Forcer l'envoi de la base de donnees erp.db"
git push origin main

A LANCER SI MIS A JOUR BLOQUER DANS LES FICHIERS TEMPORAIRES

python -c "import sqlite3; conn = sqlite3.connect('backend/data/erp.db'); conn.execute('PRAGMA wal_checkpoint(TRUNCATE);'); conn.close()"


ENUITE:

git add backend/data/erp.db
git commit -m "Fusion du cache WAL et mise a jour de la base de donnees"
git push origin main